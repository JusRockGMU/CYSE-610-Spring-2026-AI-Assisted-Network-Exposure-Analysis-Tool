# Data Directory

This directory stores scan uploads and generated reports.

- `uploads/` - Uploaded Nmap XML files
- `reports/` - Generated HTML/JSON reports

**Note:** This directory is excluded from git via .gitignore
